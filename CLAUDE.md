# CLAUDE.md — для наступних автономних сесій

## Що це

`neurica.net` — публічна експериментальна поверхня EONYX. Astro 7,
TypeScript strict, content collections + Zod, без Tailwind, без
UI-фреймворків, без бекенду в поточній реалізації. Пілот може
додати потрібний рантайм та авторизацію за docs/08-altaco-pilot.md. Деталі — `docs/00-what-neurica-is.md` і
`docs/04-architecture.md`.

## Робочі пілоти — рішення власника 06.09.2026

Neurica може нести реальне бізнес-навантаження в погодженому обсязі.
Перший пілот — каталог ALTACO, клієнтські КП і прайси на період
розробки ALTACO Platform. Канон: `docs/08-altaco-pilot.md`.
Реальні товарні дані дозволені; публічні та внутрішні дані й функції
мають різний доступ. Єдине джерело, дата даних і переносимість модулів
обов'язкові.

Стара абсолютна заборона комерційного використання скасована.
У коді 5.1 наразі ще відхиляє LIVE_BUSINESS і ALTACO + LIVE:
це відома незавершена міграція, а не чинна продуктова заборона.
Під час реалізації замінити її явними перевірками пілота, одночасно
узгодивши схему, RuntimeNotice, фікстури й тести. Не вимикати перевірки
мовчки, не маскувати реальні операції під SYNTHETIC або STATIC.
Етап 1 — каталог DEMO/SANITIZED із публічним знімком без живих
операцій (docs/09-altaco-catalog.md). Міграція 5.1 потрібна перед
LIVE-інтеграцією та захищеними комерційними операціями.

5.7 (обидві мови) та 5.8 (жодних плейсхолдерів endpoint/build_ref)
зберігаються. Публічний опис має відповідати фактично реалізованим функціям.

## Порядок кроків

Публікація об'єкта: **03 → 05** (`docs/03-publishing.md` →
`docs/03a-localisation.md` для мовної частини). Не додавати об'єкт без
обох `uk.md`/`en.md` — 5.7 однаково зупинить збірку, простіше зробити
одразу правильно.

Інформаційна архітектура (ролі сторінок, шаблони об'єкта, масштаб
картки) закрита — `docs/06-information-architecture.md`. Дизайн
головної, верстки стрічки/карток/hero виконаний і виміряний реальним
рендером (рішення 041–050, `docs/05-decisions.md`) — це вже не
відкритий крок. Подальші візуальні правки йдуть тим самим шляхом:
брифінг → зміна → вимір реальним рендером → запис рішення.

## Стан гілок

- `main` — production-гілка. Тут уже опублікований Astro-сайт і
  каталог ALTACO (PR №10, коміт `af0110b`; виправлення CI — PR №11,
  той самий коміт). Netlify публікує з `main` автоматично. Прямий
  push, merge чи force-push у `main` — лише з прямим дозволом
  користувача; звичайний шлях змін: короткоживуча гілка → PR до
  `main` → перевірки (workflow «Validate build») → окремо погоджене
  злиття.
- `claude/neurica-eonyx-relaunch-5e3glx` — гілка релончу, злита в
  `main`. Вона більше не основна робоча гілка; нові зміни йдуть через
  нові короткоживучі гілки, як описано вище.

Теги: `git ls-remote --tags origin` 18.09.2026 не показав жодного тега
на віддаленому репозиторії. Не називати `v0.1-final` чи тег відкату
(`neurica-pre-relaunch-2026-09-16`) наявними без повторної перевірки —
попередня спроба опублікувати тег відкату не вдалась (відмова прав),
а локальна наявність тега не доводить, що він є на origin.

## Посилання

- `docs/00-what-neurica-is.md` — функція, три поверхні, межа
- `docs/01-content-model.md` — модель контенту (джерело істини,
  поточна схема та план переходу)
- `docs/02-design-system.md` — invariant/directional, як (не)
  розширювати tokens.css
- `docs/03-publishing.md` — як додати об'єкт
- `docs/03a-localisation.md` — локалізація, як додати мову
- `docs/04-architecture.md` — стек, збірка, Netlify
- `docs/05-decisions.md` — журнал рішень
- `docs/06-information-architecture.md` — ролі сторінок, шаблони
  об'єкта, масштаб картки, правило двох виходів на EONYX
- `docs/07-content-spec.md` — специфікація для автора: файли, розміри
  зображень за art_direction, межі довжини заголовка, чек-лист п'яти
  стартових об'єктів

- `docs/08-altaco-pilot.md` — дозволений робочий пілот ALTACO, доступ, дані й критерії запуску
- `docs/09-altaco-catalog.md` — актуальний стан каталогу ALTACO, порядок імпорту, історія реалізації
- `docs/10-release-checklist.md` — підтверджений запуск v1 і відкриті production-перевірки
