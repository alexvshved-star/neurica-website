# Журнал рішень

| # | Рішення | Причина |
|---|---|---|
| 001 | Домен і назву збережено | етимологія не критична; перегляд через 6–12 міс. |
| 002 | Snapshot, не живі дані ALTACO | static не має шару, де приховати поля |
| 003 | Демо вбудовується артефактом, не підключається | артефакт не розходиться з джерелом і не падає разом |
| 004 | Дві осі: type і status | одна не виражає «CASE + FROZEN» |
| 005 | `/index-en` і `/en` → 301 → `/en/`, на `/` редиректу немає | старий сайт — одна сторінка у двох мовах, підсторінок продукту не було; 410 застосовувати нема до чого |
| 006 | Без Tailwind | дублює шар токенів |
| 007 | Структурні поля спільні для мов | дубльовані розходяться, 5.1 стає вибірковим |
| 008 | Таксономія не перекладається | технічний регістр, mono-трактування |
| 009 | Слаг спільний, різниця лише в префіксі | стабільні URL, тривіальний перемикач |
| 010 | uk в корені, en під `/en/` | основна аудиторія українська |
| 011 | Форм на каркасі немає | заявка про співпрацю — поведінка eonyx.net; рішення відкладене |
| 012 | `src/content.config.ts`, не `src/content/config.ts` | Astro 7 видалив legacy-розташування файлу конфігурації колекцій |
| 013 | Кастомний loader замість стандартного glob-loader'а | об'єкт складається з 3 файлів (index.yaml + uk.md + en.md), потрібна одна крос-файлова валідація на slug |
| 014 | `/work` без інтерактивного UI фільтрів на цьому кроці | NON-GOALS сесії забороняє форми й поля вводу; фільтрація за type/status — верстка кроку 05 |
| 015 | Семантичні бренд-токени (`--brand-black/navy/cyan/red`) замість шкали navy | брендбук задає чотири окремі кольори, не градації |
| 016 | **Частково переглянуто вдруге.** `card-border: none` для ORGANIC — підтверджено (без змін). Для MATERIAL — знято в межах стрічки (не в токені: `.feed :global([data-direction='material']) { border: none }`, `HomeView.astro`; компактний список зберігає токен як є). CHROMATIC і MONOCHROME лишаються відкритими | ORGANIC (перегляд 1): перша реальна обкладинка (what-neurica-is) — кроп 810×1080, абсолютний чорний `(0,0,0)` по лівих ~⅔ кадру, чорна точка навмисно не піднята (рішення 039). У картці на 1440px це чорне поле читається помітно темнішим за `--brand-navy` фон — межа картки видима без бордера. MATERIAL (перегляд 2, цей крок): після переїзду `--card-density` на підпис (рішення 041) зображення `altaco-catalog` (реальна обкладинка, не плейсхолдер) бере край картки впритул — на реальному рендері рамка (`--rule-medium solid --color-rule`, 2px/14% білого) виявилась зайвою: темний шов між фасетками фасаду сам відділяє картку від сторінкового фону, з рамкою чи без (порівняно скріншотом до/після). Override — тільки в `.feed`, не зміна дефолту токена: `--card-border` використовується лише в `WorkCard.astro`, і зміна значення в `tokens.css` зачепила б і компактний список на `/work`/`/archive`, де рамка досі виконує іншу роль (єдиний візуальний поділ між рядками однорідного переліку без cover і без різної ширини карток). CHROMATIC (`synthetic-structures-01`) і MONOCHROME (`agent-workflow`) досі на плейсхолдерах (суцільний `(0,0,0)`, перевірено `sharp .stats()`) — той самий висновок туди не переноситься: плейсхолдер дає хибну впевненість (будь-яка рамка на суцільному чорному виглядає непотрібною / чорний кут проти navy читається так само, як реальне ORGANIC-фото), реальних зображень для чесної перевірки ще немає. Перевірити обидва, коли з'являться реальні обкладинки — не раніше |
| 017 | `--weight-body: 400` (Regular) підтверджено, не 300 (Light) — окремо від lead | прохід v0.2 перевірив lead і body **окремо**, реальними умовами (`--width-reading` 680px, `--leading-body` 1.6, `--color-text` на `--color-bg`, обидві мови, `/dev/type` розділи 4–5, Playwright, `deviceScaleFactor: 2`): при кеглі lead (1.25rem) 300 тримається — товщий штрих компенсує тонку вагу. При кеглі body (1rem) 300 помітно тонший і послаблює форму літер (особливо кириличних, де багато тонких штрихів), 400 лишається суцільним. Легібність ваги залежить від кеглю, не єдине правило "300 чи 400 взагалі" — тому lead: `--weight-lead` (300), body: `--weight-body` (400) — різні токени, різні висновки |
| 018 | `/archive` — тільки `ARCHIVE`, `FROZEN` лишається в `/work` | попередній бриф ставив `FROZEN` в архів, що суперечить 5.3: «сторінка живе далі» означає лишитися на видноті, не переїхати в архів |
| 019 | `published` (ISO-дата) замінює `year` як структурне поле | `year` не задає порядку — два об'єкти того самого року нічим не впорядковуються; `year` для показу тепер виводиться з `published` |
| 020 | `/work/[type]` зарезервовано, не реалізовано в v1 | при п'яти об'єктах фільтр — UI без задачі; статичні `/work/[type]` індексуються й не потребують полів вводу, коли з'являться |
| 021 | `feature` — єдине ручне поле масштабу картки, максимум 3 | масштаб (large/medium/small) виводиться з feature + type/status; понад 3 featured — warning на збірці, не fail |
| 022 | Правило 5.8 — заборона плейсхолдерів у `endpoint`/`build_ref` | `altaco-catalog` вів на неіснуючий домен; RuntimeNotice захищає від "дані непевні", не від "посилання мертве" |
| 023 | Перевірка доступності endpoint — окремий не блокуючий крок CI | зовнішній збій не повинен зупиняти збірку сайту, але має лишатись видимим до першого відвідувача |
| 024 | `media[]`: `{id,src}` структурно, `{alt,caption}` локалізовано за тим самим id | те саме розділення, що й для решти полів моделі; кожен id обов'язковий в обох мовах, як 5.7 для перекладів |
| 025 | `h1` і `.type-display` розділені: h1 = `--type-h1`/weight-strong, display = `--type-display`/weight-display, лише для знака `NEURICA` в hero | до цього жоден з двох не мав `font-size` узагалі й падав на дефолт браузера; display 900 на заголовку сторінки був би занадто важким |
| 026 | Компоненти сторінок використовують `var(--section-gap)`, не `var(--section-gap-mobile)` | `--section-gap` вже адаптивний (сам перемикається на mobile-значення в tokens.css); хардкод mobile-значення напряму означав, що desktop отримував той самий тісний відступ, що й mobile |
| 027 | `--tracking-label: 0.14em` лишається спільним для uk/en, без окремого правила для кирилиці | перевірено реальним рендером (`/dev/type`, Playwright): кирилична все-капс мітка не розсипається при 0.14em; production сьогодні кирилицю крізь `.meta` й так не жене (таксономія завжди англійська, рішення 008) |
| 028 | 600 (semibold) — новий дефолт `--weight-h1`/`--weight-h2`; 700 (bold) лишається окремою роллю `--weight-strong` тільки для дрібного акцентного заголовка | 700 на h1/h2 (успадковане від старого `--weight-strong`, використаного помилково для обох ролей) було важче за брендбук-рекомендацію; 600 — значення, яке брендбук власне й рекомендує для заголовків. 700 не видалено: дрібний знак `NEURICA` в шапці/підвалі на малому кеглі оптично "розсипається" при 600, тож там навмисно лишається компенсація вагою — не та сама роль на іншому кеглі, тому окремий токен, не варіант h1/h2 |
| 029 | Дескриптор hero й напис бренду (`by EONYX` у шапці/підвалі) переведені з mono на Montserrat | обидва — тест голос/механіка дав "голос": дескриптор — речення бренду ("AI experiments, prototypes and selected work by EONYX"), не стан і не структура; `by EONYX` — tagline, не таксономія. До проходу v0.2 обидва помилково успадковували моноширинну гарнітуру (дескриптор — прямий клас `mono` на `<p>`; `by EONYX` — через бланкетний `class="mono"` на всьому nav/footer блоці, який у підвалі ще й примусово капіталізував напис бренду). Теги об'єкта (`work-detail__tags`) переведені у зворотному напрямку, з `.small` на `.mono` — той самий тест дав "механіка": категоріальні метадані, не голос |
| 030 | `--chevron-angle`/`--chevron-thickness`/`--chevron-gap` видалені з `tokens.css` | замінені оригінальними SVG-контурами з брендбука (`src/assets/brand/eonyx-chevron.svg` та ін.) — знак тепер файл-оригінал, не фігура, побудована з параметрів; підставляти токени більше нікуди. Виміряні з контурів значення (кут 34,925°, товщина 0,1816 від висоти, засічка 0,5 висоти) задокументовані в `02-design-system.md` як довідка, не токени |
| 031 | Фавікон (`public/favicon.svg`, `apple-touch-icon.png`) — композитний `eonyx-mark` (композиція 5), не ізольований `eonyx-chevron` | одиночний шеврон без слогану не відповідає жодній із п'яти затверджених композицій брендбука (4 — шеврон+слоган, 5 — повний `>/\`) і фактично був шостою, некерованою композицією; виправлено після уточнення переліку композицій — mark уже підтверджений як "вживається на Neurica" (композиція 5, поки як актив), тому саме він, не новий лок-ап. Компроміс: на 16px деталі mark зливаються сильніше, ніж в ізольованого шеврона (перевірено растеризацією `sharp` на 16/32px) — затверджена композиція взята пріоритетнішою за читабельність на найменшому розмірі. Білий на `--brand-black`, apple-touch-icon 180×180 через `sharp` із того самого SVG |
| 032 | `--tracking-wordmark: 0.06em` — новий токен, `--tracking-display` (-0.03em) не змінено | подача hero за зразком ALTACO MONOCHROME: позитивний трекінг на `NEURICA` (Montserrat 900, латиниця). `--tracking-display` лишається спільним рівнем шкали для `.type-display` — той самий негативний трекінг, що й до цього кроку; позитивна розрядка не переноситься на кириличні H1 (сторінки/об'єкти перекладаються), бо на кириличних капітелях вона розтягує форму літер, а не робить її чіткішою, як на латиниці. Застосовано компаунд-селектором `.type-display.hero__wordmark`, тільки до слова `NEURICA` в hero головної. Другий рядок лок-апа ("experiments", Montserrat 700, tracking 0.26em) — голос бренду, не таксономія: залишений на Montserrat, не переведений у mono, за тим самим тестом голос/механіка, що й `by EONYX`/дескриптор (рішення 029). Коефіцієнт `margin-top` другого рядка (0,151 × `--type-display`) виміряний реальним рендером (canvas `TextMetrics` у Chromium, не абстрактний розрахунок) так, щоб базова лінія лягала на 0,83 × cap-height `NEURICA` нижче — похибка ≈0,03px на 1440px |
| 033 | Hero отримує `data-direction="chromatic"` (не ORGANIC), червоний акцент на цьому екрані не використовується; чорна точка зображення підняте до `#101820` | ORGANIC — "змія, пір'я, скульптурні обличчя" (природні патерни), нічого спільного з архітектурним неоновим коридором; CHROMATIC уже задокументований у `tokens.css` як "рожево-cyan коридор... найсильніший entry point" — точний опис цього зображення. У кадрі домінує маджента (виміряно, не з опису), поруч `--brand-red` читається як помилка друку — CHROMATIC дає cyan-акцент структурно, жоден елемент hero не звертається до `--brand-red`. Чорна точка: зображення мало абсолютний `(0,0,0)`, сторінка — `#101820`; піднято `sharp .linear(scale, offset)` (levels-зсув тіней, `input=255→output=255` точно, світла не займані) — перевірено піпеткою на кутовому пікселі після обробки: рівно `#101820`. `--image-overlay` (стандартний для CHROMATIC-обкладинок карток) свідомо не застосований до hero: реальний вимір контрасту (canvas + WCAG-формула на фактичних пікселях тексту зони, не абстрактно) дав 14.45:1 для wordmark/eyebrow (білий) і 5.37:1 для дескриптора (`--color-muted`) — обидва з запасом понад AA 4.5:1, градієнт лише забруднив би кадр |
| 034 | Мобільний hero (< 768px) — зображення взагалі не рендериться, чисте `--color-bg`, не кроп-варіант із затемненням | обидва варіанти побудовані й виміряні реальним контрастом на дескрипторі: кроп правої третини як напівпрозорий фон (`opacity` знижена до порогу проходу WCAG AA) при 0,35 давав 2.89:1 (провал), тільки при 0,13 — 4.71:1 (прохід), і на такому затемненні зображення практично не читається — крихкість тюнінгу під конкретне зображення не виправдала слабкий візуальний ефект. Чисте `--color-bg` не залежить від контенту зображення, надійне за конструкцією, і відповідає мінімалістичній подачі решти мобільних екранів сайту. `sizes="(max-width:767px) 1px, 100vw"` запобігає завантаженню повнорозмірного зображення, коли воно приховане |
| 035 | Центрування — `data-align="center"` на `<main>` (`WorkDetailView.astro`), не `text-align` усередині вузького блоку прози | попередній прохід центрував лише `.editorial-template` (текст усередині своєї колонки), а заголовок/метадані (спільні для всіх типів, рендеряться у `WorkDetailView`) лишались по лівому краю — дві різні осі на одній сторінці, порожня третина зліва. Це не "половинчасте центрування, яке можна залишити" — колонка прози й колонка заголовка мають бути на тій самій вісі. `data-align="center"` для `entry.type` ∈ {EDITORIAL, VISUAL_STUDY} центрує `.work-detail__header` і `.work-detail__pager` (`margin-inline: auto`, та сама міра 560px, що й у самому шаблоні) — одна вісь на всю сторінку, включно з мобільним (там колонка й так на всю ширину, але заголовок/pager раніше лишались ліворуч, а проза — вже центрованою; та сама половинчастість, менш помітна без порожньої третини, але реальна — виправлено на всіх брейкпойнтах) |
| 036 | **Переглянуто.** Центрування — наскрізне для всієї прози сайту (усі типи об'єкта, `/about`, `/404`), не вибіркове за типом. Виняток — форма контенту, не тип: переліки карток (`/work`, `/archive`) і моноширинні формули. `summary`, що містить формулу зі стрілками (`inventory → catalog → PDF`), лишається `.mono` і завжди ліворуч (`.work-detail__summary--formula`, перекриває успадкований центр) незалежно від сторінки — mono означає машинну інформацію, а її вирівнюють ліворуч; центрований моноширинний рядок читається як зламаний код. `--width-reading` (680px) далі не змінюється, лишається джерелом істини без активних споживачів на цьому кроці | Первісне рішення (CASE/PROTOTYPE ліворуч — найдовший текст сайту й посилання-дія псують читабельність/виглядають випадково центрованими) переглянуто після візуальної оцінки: на реальному рендері вибіркове центрування читалось як непослідовність системи ("чому саме ці два шаблони інакші?"), а не навмисний контраст із вагомим поясненням. Довший текст і посилання-дія лишаються дійсними міркуваннями, але недостатніми, щоб виправдати два вирівнювання в одній системі — переважило "один почерк для всієї прози сайту", той самий принцип, що вже застосований для гарнітур і кольору. RuntimeNotice (демо-блок) центрується як цілісний блок (позиція на осі), внутрішній текст лишається ліворуч — так природніше читається notice-панель зі статусом і посиланням |
| 037 | `/about`, `/404` — окремий внутрішній блок (`.about-content`, `.not-found-content`) під центрування, не `main` сам | `main` уже мав горизонтальний `padding: var(--grid-margin)` для зовнішнього відступу; додати `max-width: 560px; margin-inline: auto` на той самий елемент означало б відняти `grid-margin` ще раз усередині вже звуженої 560px колонки — реальна текстова колонка вийшла б помітно вужчою за задумані ≈560px (перевірено розрахунком, 560 − 2×32px = 496px на десктопі). У `WorkDetailView.astro` цієї проблеми нема: `main`-падінг і 560px-колонка вже на різних елементах (main і `.work-detail__header`/шаблон відповідно) |
| 038 | Знак ALTACO (`altaco-wordmark`, `BrandMark.astro`) — позначка контексту над `<h1>` на сторінці об'єкта, рендериться з мапи `context → знак`, не хардкодом; висота = висота `.meta` (`--type-metadata`, дає ≈120px ширини при 10.87:1); у заголовок і на картки не вставляється | Поле `context` уже структурне в схемі (`ALTACO`/`EONYX`/`EXTERNAL`/`INTERNAL`) — знак стає його візуальним представленням, не окремим рішенням для однієї сторінки. Мапа (не `if (context === 'ALTACO')`) — щоб додати EONYX пізніше означало дописати рядок, не чіпати умовну логіку шаблону; `EXTERNAL`/`INTERNAL` без знака, бо для них нема запису, не бо є явна заборона. Не в заголовку: капітелі чужого wordmark'а мають іншу вагу/пропорції/розрядку, ніж Montserrat SemiBold — вставка в текст title виглядала б чужорідно. Не на картці: 10.87:1 на карточному масштабі перетворюється на нерозбірливу смужку, той самий аргумент, що вже застосований до EONYX-mark на фавіконі (рішення 031). `currentColor` успадковує `--color-muted` — перевірено реальним рендером на MATERIAL (справжній напрям altaco-catalog) і тимчасово на CHROMATIC (найтемніший фон, `--black`): контраст ≈6.84:1, вище WCAG AA. Доступність: `aria-hidden` на знаку, `.sr-only` з літералом `entry.context` поруч, скрінрідер читає "ALTACO" |
| 039 | `what-neurica-is`: `art_direction` MONOCHROME → ORGANIC | Перша реальна обкладинка (бронзова/патинована скульптурна голова, теплий teal з золотими виблисками) — не архітектурний ч/б кадр, а органічний матеріал/текстура; MONOCHROME (за токенами — холодний контрастний ч/б) більше не описує зображення, ORGANIC описує точно. Побічний ефект: акцент об'єкта зміщується з `--brand-red` на `--brand-cyan` — cyan сидить поряд із патиною природніше, ніж червоний |
| 040 | `organic-intelligence` видалено повністю (директорія, обидва переклади, media-плейсхолдери) | Це був структурний плейсхолдер без реального вмісту (обидва media — тільки alt-плейсхолдер, без фото). Після рішення 039 `what-neurica-is` зайняв ORGANIC — два ORGANIC-об'єкти серед 5 фікстур порушували б правило чергування 5.6. Стартовий набір — тепер чотири об'єкти: `altaco-catalog`, `agent-workflow`, `synthetic-structures-01`, `what-neurica-is`. Перевірено після видалення: `/`, `/work`, sitemap (`sitemap-0.xml`) і prev/next-ланцюжок на сторінках об'єктів — жодних згадок `organic-intelligence`, ланцюжок замикається коректно на чотирьох об'єктах |
| 041 | Стрічка на `/` — editorial wall на flexbox (`HomeView.astro`), не CSS Grid у 2 колонки: `--row-height` за `data-scale` (large/medium/small) живить `flex-basis`/`max-width` картки за формулою `row-height × --image-ratio`, flex-grow:1 порівну на всіх картках рядка заповнює рядок по ширині. Масштаб більше не міняє кількість колонок (колонок немає взагалі) — тільки цільову висоту рядка. `max-width: flex-basis × 1.5` — страховка проти картки-одиначки на власному рядку (інакше рівний flex-grow розтягнув би її на всю ширину контейнера й, через aspect-ratio, по висоті теж, ламаючи 70vh). Мобільний (≤768px, той самий поріг, що й в решті tokens.css): рядок вироджується в стовпчик, `max-width` на самому зображенні = `(70vh − 14rem) × ratio` — резерв 14rem під підпис, підібраний реальним виміром найгрубішого підпису (ORGANIC/CHROMATIC `--card-density: --space-12`, 2×48px, + дворядковий заголовок). І на десктопі, і на мобільному застосовано той самий принцип за пріоритетом: пропорція `art_direction` ніколи не ламається (ширина завжди = висота × ratio, ніякого object-fit-кропу понад штатний), 70vh — тверда стеля на всю картку (зображення + підпис), "рядок заповнює ширину контейнера" — бажаний результат, що поступається двом попереднім, коли вони конфліктують (картка-одиначка, вузька портретна ORGANIC на короткому viewport). Усі числа (`min(Xvh, Ypx)` за `--row-height`) підібрані ітеративно реальним рендером (Playwright `getBoundingClientRect`, не абстрактний розрахунок) на 375/768/1440/1680, обидві мови — фінальні значення: base `min(24vh,300px)`, large `min(28vh,400px)`, medium `min(28vh,340px)`; на 1440px усі чотири об'єкти лягають в один рядок, повна сторінка ≈1.9 екрана замість попередніх 4+ | Стара сітка (2 колонки, `large` = `grid-column: span 2`) при портретній ORGANIC-обкладинці (рішення 039) розтягувала картку на понад дві висоти екрана — ширина колонки й пропорція зображення більше не пов'язані. `--card-density` (`work-card` padding) переїхав із самої `.work-card` на `.work-card__body` (`WorkCard.astro`) — зображення тепер бере край картки, `--color-surface` лишається тільки під підписом; компактний список (`/work`, `/archive`) отримує явний override, що відтворює точний попередній padding (`.work-card--compact .work-card__body { padding: 0 }`, зовнішній `padding: var(--space-4)` на `.work-card--compact` не чіпався) — жодної візуальної зміни там |
| 042 | `/work` — вузький список (`WorkIndexRow.astro`, новий компонент), не дублікат стрічки з `/` | Раніше `/work` рендерив ті самі повні картки, що й `/`, лише без cover — два URL з майже однаковим виглядом. Design-brief просив рядок-метадані: мініатюра 72×54/88×64, `TYPE`, заголовок, summary в один рядок з `text-overflow: ellipsis` (не перенос), рік праворуч, тонка `--color-rule`-лінія між рядками — не повноширинні `--color-surface`-смуги, як було. Колонка ~880px, `margin-inline: auto` (та сама вісь центрування, що й решта сторінок сайту, лише своя міра — списку потрібно місце під мініатюру+текст+рік в один рядок, не 560px прозової колонки). Окремий компонент, не третій режим `WorkCard.astro`: `/archive` і далі використовує `WorkCard` `compact` без жодної зміни — спільний CSS-клас ризикував би зачепити сторінку, яку цей крок явно не мав чіпати |
| 043 | Обкладинка сторінки об'єкта (`.work-detail__cover`, `WorkDetailView.astro`) — на всю ширину контейнера, `object-fit: cover` замість `max-width: var(--width-content)` без cover-поведінки | Раніше кадр тримався власної натуральної ширини (до стелі 1200px) без `width:100%` — на широких viewport порожній `--color-surface` лишався праворуч від кадру. `aspect-ratio: var(--image-ratio)` + `object-fit: cover` — той самий рецепт, що вже в `WorkCard.astro` для карток стрічки: кроп без літербоксингу. Виміряно реальним рендером на 1440px: `altaco-catalog` (MATERIAL) — кадр точно 1376×1376 (контейнер мінус `--grid-margin`), 1:1, нуль порожньої поверхні |
| 044 | Свідомий `object-position: right center` для ORGANIC-обкладинок на стрічці (`WorkCard.astro`, `[data-direction='organic']`) | Сюжет портретного 3:4 кадру (what-neurica-is) розташований праворуч (докладніше — крок "перша обкладинка what-neurica-is"). Сьогодні кроп точно збігається з `--image-ratio` (0% відхилення), тож видимого ефекту нема — це страховка на випадок, якщо наступна ORGANIC-обкладинка не буде кадрована так само точно й реально потребуватиме кропу під час object-fit:cover |
| 045 | **2026-08-28. Hero — третя ітерація, тільки текст.** Неоновий коридор (CHROMATIC-фон, рішення 033/034) прибрано повністю, `src/assets/hero/entry.png` видалено з репозиторію. Локап (`NEURICA` / `experiments` / дескриптор) — той самий трирівневий рецепт, однаковий на mobile і desktop, без брейкпойнт-залежної поведінки. Головна: замість editorial wall (рішення 041) — фіксована сітка 2×2, `aspect-ratio:1/1` на всіх чотирьох клітинках **незалежно від `art_direction`**, блок `min(720px, 92vw)` по центру, не розтягується на всю ширину. Новий компонент `HomeGridCell.astro` — `WorkCard.astro` тепер викликається лише в `compact`-режимі (`/archive`); його повний (не-compact) режим і `cardScale()` нізвідки більше не викликаються, свідомо не видалені цим кроком (див. звіт) | Рішення дизайнера, не технічний компроміс — третя, свідома ітерація hero після текстового і CHROMATIC-фонового варіантів. **Наслідок для системи, зафіксовано, не обійдено:** `--image-ratio` перестає бути ознакою напрямку в стрічці головної — чотири напрямки там тепер розрізняються тільки `--color-accent` і `--image-filter`, не формою кадру. Токен `--image-ratio` сам не змінено і лишається чинним там, де раніше: сторінка об'єкта (`WorkDetailView.astro`) зберігає пропорцію напрямку для обкладинки-плити. Детальний опис — `docs/02-design-system.md`, розділ «`--image-ratio` більше не діє на головній» |
| 046 | **2026-08-28. Пропорції й типографіка підписів сітки головної.** Клітинка звужена до ≈19,5% ширини viewport на 1440 (≈280px, було 22,6%/325px), блок сітки — `min(590px, 92vw)` (було 660px), власний `margin-inline:auto` окремо від `.page-column` (660px, hero не чіпається — той самий "різна ширина, одна вісь" прийом, що вже в `WorkDetailView.astro`). `column-gap:30px`; вертикальний ритм асиметричний і керується `margin` на `.grid-cell__caption` (`HomeGridCell.astro`), не `row-gap` на `.feed__grid` — CSS Grid не дає різних `row-gap` для різних меж рядків: `margin-top:14px` (фото→підпис), `margin-bottom:85px` (підпис→наступне фото, знято для останнього ряду). Заголовок клітинки — mono (`--weight-bold`, 0.8125rem, `letter-spacing:0.08em`, uppercase, `--color-text`), не Montserrat: виняток із правила "Два регістри" (docs/02-design-system.md), звужений до цієї єдиної клітинки. `JetBrains Mono` вага 700 додана в `Base.astro` (раніше лише 400) | Design-brief явно попросив звузити клітинку під референс (19,5%, не поточні 22,6%) і розвести gap по осях асиметрично — вертикальний проміжок більший, бо в ньому стоїть підпис попереднього ряду, "дає композиції повітря". Підпис-mono — тому що в сітці він працює як етикетка експоната під фото (TYPE + назва читаються як один технічний рядок метаданих), не як заголовок сторінки — контекст відрізняється від H1/`/work`/`/archive`, де назва лишається голосом бренду (Montserrat) без змін. Перевірено реальним рендером (Playwright `getBoundingClientRect`) на 375/768/1440/1680, обидві мови: клітинка точно 280×280 на 1440/1680 (19,4% від 1440 — практично точний збіг із референсом), `column-gap` 30px, `margin-top` (фото→підпис) 14px, `margin-bottom` (підпис→наступне фото) 85px — усі три без відхилень на жодному брейкпойнті. Найдовший заголовок (`ДИНАМІЧНИЙ КАТАЛОГ ALTACO`, кирилиця, все-капс) уміщається в **один** рядок навіть у найвужчій 280px клітинці (виміряно `Range.getClientRects()`) — зменшення кеглю, передбачене брифом на випадок переносу на третій рядок, не знадобилося |
