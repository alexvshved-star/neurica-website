# Каталог ALTACO — актуальний стан

Стан узгоджено 13.09.2026; назви трьох варіантів Azul Aran та Black Tempest Satin уточнено 15.09.2026. Цей розділ описує чинну поведінку;
датовані розділи нижче зберігають історію і не замінюють цей опис.

## Що працює зараз

- 86 товарних позицій: 84 сляби та 2 фрагменти. П'ять колишніх зразкових
  слябів продаються як звичайні натуральні матеріали; ручні зразки виключено.
- Фото є для всіх 86 позицій, 76 різних файлів. Grigio Nube — 300×188 px,
  потрібен більший оригінал. Фото передає фактуру, не гарантує малюнок слябу.
- Шість полів: пошук, виробник/колекція (family), тип, обробка, товщина,
  наявність. Окремого фільтра колекції і керування сортуванням немає.
  Порядок: наявні першими, далі назва. Колір відкладено.
- За поточним знімком 78 позицій мають ознаку наявності, 8 — під замовлення.
  Це не числові фізичні залишки і не підтвердження складу на сьогодні.
- Дві мови, каталог і окремі сторінки матеріалів. Кошик у localStorage
  зберігає лише ID та кількості; між пристроями не синхронізується.
- SM Quartz: крок 0,5 слябу; натуральні сляби — цілі. Ціни роздрібні,
  EUR із ПДВ, без знижок; ціна половини — 50% ціни цілого слябу.
- КП на 1–20 позицій і два прайси вже формуються в браузері зі спільного
  snapshot. Фрагменти й позиції під замовлення блокують готову КП.
  Натуральний прайс: 49 позицій / 11 сторінок; SM Quartz: 29 / 7.
- Є перегляд, завантаження й поширення PDF. iPhone перевірено власником;
  Android не перевірено. Зміна кошика скидає попередній PDF.
- Телефон PDF: +38 097 242 21 21, спільний contact.mjs.
- importedAt: 2026-09-06T18:29:49Z; dataAsOf: null. Каталог, сторінки
  матеріалів і PDF показують непідтверджену актуальність. За підтвердженої
  дати показується тільки «Залишки станом на …». Дата імпорту й дата
  формування PDF не підміняють дату актуальності.

## Чинний порядок імпорту

Джерело — аркуш «Наявність», експорт UNFORMATTED_VALUE з результатами
формул у приватний JSON `{ "values": [...] }`. Діапазон повинен охоплювати
всі товарні секції та рядки; старе A1:V110 не є постійною гарантією повноти.
Перед імпортом звірити джерело, особливо погоджені ціни та обробки.
Ціни беруться заново з таблиці: правка тільки snapshot не захищена від
наступного імпорту. ID, фото й метадані зіставляються через manifest;
невідомий ключ зупиняє імпорт. Silk додається за обробкою поверхні.

```sh
node scripts/catalog/import-stock.mjs /private/stock.json ACTUAL_ISO_IMPORT_TIME
node --test scripts/catalog/*.test.mjs
npm run build
```

ACTUAL_ISO_IMPORT_TIME замінити фактичним часом читання. Необов'язковий
третій позиційний аргумент — підтверджена календарна дата YYYY-MM-DD.
За відсутності підтвердження залишити його порожнім: dataAsOf буде null.
Календарна дата перевіряється на існування, включно з високосними роками.

Перед записом import-guard.mjs порівнює ID з попереднім валідним snapshot.
Зникнення будь-якої позиції за замовчуванням блокує заміну, навіть якщо
загальна кількість не змінилася. Нульовий залишок не видаляє позицію.
При помилці перевірок старий файл лишається незмінним.

Якщо видалення справді погоджене, записати підставу в журналі рішень,
підготувати приватний JSON-масив точних публічних ID і додати
`--removed-ids=/private/approved-ids.json`. Перелік має точно збігатися зі
зниклими ID: зайві, повторені й непогоджені ID відхиляються. Це не загальний
прапорець обходу перевірки; файл сам собою не є доказом погодження.
Імпортер вимагає наявний валідний snapshot; початкова міграція — окрема дія.
Захист не доводить актуальність фізичного складу або правильність усіх цін.
Сирі рядки, точні залишки й внутрішні ціни не комітити та не друкувати.

## Перевірки та відкриті питання

43 каталожні тести: включно із запуском CLI у тимчасовій копії,
збереженням старого snapshot після обрізаного експорту, точним погодженням
видалень і відхиленням неіснуючих дат. Тести не дорівнюють повному
візуальному чи мобільному прийманню. Негативні src/_fixtures/ досі ручні.

Відкриті: звіряння складського джерела та дати відповідного знімка,
Android, оригінал Grigio Nube, окремий розгляд залежностей. Бекенд,
авторизація, резервування й перенесення в ALTACO Platform відкладені.

## Історія реалізації

15.09.2026: за дорученням власника Black Tempest / 2026 перейменовано в
Black Tempest Satin у джерелі («Наявність»!A48), snapshot і ключі manifest.
ID nat-346630f20f40, Satinato, 3300×1950×20 та решта товарних полів
збережені. Варіант Black Tempest 2022 з обробкою Polished не змінено.
Назва спільна для обох мов сайту та нових КП/прайсів. Стару назву
збережено в журналі для простежуваності. Це не новий імпорт або звірка
складу; importedAt/dataAsOf залишаються незмінними.

15.09.2026: за дорученням власника Azul Aran B055958 перейменовано в
Azul Aran NEW у джерелі («Наявність»!A22), snapshot і ключі manifest.
ID nat-120e46dfe4a5, Polished, 3570×1500×20 та решта товарних полів
збережені. Попередня назва з кодом B055958 збережена тут як історія
зіставлення, а не нове поле лота. Тест повторного імпорту та даних
КП/прайсу охоплює всі три перейменовані варіанти Azul Aran.
Це точкова правка назви, не новий імпорт чи підтвердження залишків.

15.09.2026: за дорученням власника Azul Aran перейменовано в
Azul Aran Polished у джерелі (аркуш «Наявність», A21), snapshot і ключі manifest.
ID nat-d3042ab60764, Polished, 2780×1930×20, фото, ціни та ознака наявності
збережені. Інші варіанти Azul Aran не змінені. Тест повторного імпорту й
даних КП/прайсу охоплює обидві погоджені нові назви. Це не звірка складу;
importedAt і dataAsOf не змінені.

15.09.2026: за дорученням власника Azul Aran 2024 перейменовано в
Azul Aran Satin у джерелі (аркуш «Наявність», A34), snapshot і ключі manifest.
ID nat-43fffeeacf56, Satinato, 3200×1750×20, фото, ціни та залишок збережено.
Нова назва спільна для обох мов сайту та нових PDF; старі завантажені PDF
не змінюються. Це точкова правка назви, не новий імпорт або звірка складу.
Тест azul-aran-name.test.mjs перевіряє повторний імпорт і дані КП/прайсу.

Нижче збережено попередні описи. Твердження про 78 фото, відсутні фото,
старі фільтри, майбутні PDF та обмеження початкового етапу є історичними.
Для чинних правил використовувати розділи вище.

# Історична база — етап 1, 06.09.2026

06.09.2026. Дві мови, каталог `/work/altaco-catalog/` та окремі маршрути
`/work/altaco-catalog/<variant-id>/` (англійська — з `/en`).
Дизайнерська структура адаптована до Neurica: темний фон, чинні шрифти,
квадратні фактури, 2 колонки на мобільному, 3 на широкому екрані.
Глобальні стилі та інші сторінки не змінюються. WorkDetailView отримав
іменований slot; лише маршрути каталогу заповнюють його Catalog.
Умова за slug визначає маршрут інструмента, не виняток типографіки.

## Що працює

- Пошук за назвою, кодом, виробником/колекцією й обробкою.
- Фільтри: натуральний камінь / Santa Margherita, тип, колекція,
  обробка, товщина, наявність; сортування за назвою та ціною за м².
- URL зберігає фільтри; скидання та стан без результатів.
- Картка варіанта: формат, обробка, тип, роздрібні EUR/м² та EUR/слеб,
  індикатор наявності, фактура, джерело і посилання mailto до ALTACO.
  Для фрагмента друга ціна підписана «за фрагмент».
- 86 позицій імпорту. **Історично** (до 12.09.2026) 5 із них були
  окремою категорією «зразкові слеби», вимкненою типовим фільтром —
  розділ «Шість полів та продаж колишніх зразкових слябів» нижче
  замінює це: усі 86 тепер звичайні товарні позиції без прапорця
  зразка. Малі ручні зразки не імпортувались і не імпортуються.
- Без JS доступні всі 86 позицій і всі сторінки матеріалів. Фільтри
  потребують JS; є видиме пояснення noscript.
- 78 позицій мають фактури з двох наданих власником прайсів (69 окремих
  фото). Зображення проходять Astro Picture, AVIF/WebP та responsive widths.
  Macro не означає фото конкретного слеба; це пояснено в картці.

## Дані та відтворення імпорту

`src/catalog/model.mjs` — незалежні від Astro нормалізація, перевірка
публічного контракту й пошук/фільтрація. `types.ts` / `model.d.mts` — типи.
`manifest.json` — перевірене зіставлення ключа варіанта з ID та фото.
Ключ: секція таблиці + вихідна назва (trim) + обробка + три розміри.
ID не залежить від номера рядка, ціни чи залишку. Якщо назва/формат
у джерелі змінюються, людина звіряє та переносить наявний ID в manifest;
нові ключі автоматично не приймаються. Різні партії в назві та формати
збережені окремо. Коли джерело отримає власні SKU, їх слід зіставити
з чинними ID перед міграцією.

`src/catalog/snapshot.json` — лише публічна проєкція; читається
каталогом і карткою, надалі може використовуватися модулем PDF.
Ціни записані цілими євроцентами; значення за м² та слеб беруться
окремо з Q та S, а не відновлюються одне з іншого після округлення.
Це відображення цін, ще не калькулятор чи юридично готова КП.

Імпортувати локально з приватного JSON експорту конектора `{ "values": [...] }`
аркуша «Наявність», A1:V110 (межу збільшити при додаванні товарних рядків):

```sh
node scripts/catalog/import-stock.mjs /private/stock.json 2026-09-06T12:00:00Z
node --test scripts/catalog/catalog.test.mjs
npm run build
```

Час у прикладі замінити фактичним часом успішного читання. Не ставити дату
з назви таблиці як підтверджену дату обліку. Необов'язковий третій аргумент —
дата актуальності, лише якщо підтверджена власником джерела. Не імпортувати
FORMATTED_VALUE (ціни-рядки): потрібні UNFORMATTED_VALUE та результати формул.

Перед запуском експорту перевірити заголовки/розміщення колонок, як при
початковому зіставленні A/B/F/G/H/I/N/Q/S. Імпортер перевіряє назви ключових
колонок і зупиняється при перестановці; зміна структури потребує ревю.
Сирий файл з точними залишками, резервами й закупівельними цінами лишається
поза репозиторієм. Не друкувати рядки або сирі значення в CI/публічні логи.
Помилка/невідомий ключ зупиняє імпорт до заміни останнього знімка.
У CI автоматично запускаються тести публічної проєкції та фільтрів.

Поля кольору та кислотостійкості зі складу не публікуються: їхні значення
не пройшли перевірку. Тому фільтра кольору зараз немає. Kitchen Selection
не трактується як порода: типи зіставлені з підписами карток прайсу.
Збіги назв нормалізовано під час ручної підготовки manifest (напр.
Arabescatto/Arabescato, Sabia/Sabbia); зіставлено також формат і поверхню.
Ціни PDF не імпортуються — джерелом роздрібних цін є складська таблиця.

## Відомі уточнення

- Ціни містять ПДВ — підтверджено власником 07.09.2026. Дата актуальності джерела null,
  дата успішного імпорту відображається. Наявність >0 не публікує кількість
  і не обіцяє продаж цілого слеба при частковому залишку.
- Фото відсутні для Vittoria Regia 2025, Travertino Tipo Classico,
  Carnia, Istria, Liskamm, Metropolis Galaxy та двох варіантів Vega.
  Усі вони мають нульову наявність у цьому імпорті; зображення не підмінені.
- Бренд/колекція публікується там, де підтверджені у прайсі; не всім
  натуральним матеріалам автоматично приписано виробника Bagnara.

## Наступний етап

Контрольований робочий доступ команди, узгоджені правила кількостей,
округлення для частин слеба, автоматичне
оновлення даних. Немає кнопок із удаваною генерацією PDF,
бронювання, оплати чи серверних робочих операцій. Правила LIVE лишаються
відомою незавершеною міграцією docs/08-altaco-pilot.md; для нинішньої
DEMO/SANITIZED вітрини схема не потребує вимкнення цих перевірок.

## Публічний підбір — 07.09.2026

Цей опис історичний: із 12.09.2026 підбір замінено кошиком (див. нижче).

Додано вибір кількох матеріалів на сторінці каталогу. Підбір живе лише
в пам'яті поточної сторінки: не передається серверу, не зберігається
у браузері й очищується при переході чи перезавантаженні. Фільтри
не очищують підбір. Без JS кнопки підбору приховані.

Для цілих слебів можна ввести цілу кількість 1–999. Це межа поля,
не підтверджений залишок. Розрахунок використовує ціну слеба зі знімка
у євроцентах, без перерахунку з округленої ціни м². Резерву немає.
Фрагменти та непідтверджені ціни залишаються «за запитом»; показана
сума явно виключає їх. Зразкові слеби до підбору не додаються.

Підбір є публічною попередньою оцінкою. Додано неперсоналізовану PDF-КП;
немає клієнтських полів, знижок та внутрішніх цін. Ціни містять ПДВ; податок повторно не додається. Захищені функції команди
залишаються наступним етапом за docs/08-altaco-pilot.md.
Незалежний модуль estimate.mjs перевіряється разом із тестами імпорту.

## Цінове правило поточного етапу

07.09.2026 власник підтвердив: лише роздрібні ціни з ПДВ, без знижок.
Це правило поширюється на каталог, підбір та майбутні КП й прайси PDF.
Гуртові ціни й керування знижками не реалізуємо на цьому етапі.
Питання знижок закрите для поточного обсягу й не блокує PDF.

## Генератор PDF-КП — 07.09.2026

Шаблон: наданий власником `ALTACO_Commercial_Offer_Mystic_Grey_Lot_2025-2.pdf`.
A4: темна обкладинка з повним логотипом ALTACO і загальною сумою,
далі окрема світла сторінка для кожного матеріалу: макрофото, формат,
площа, колекція, ціни за м²/слеб та сума позиції. Українська/англійська.
Суми з ПДВ, без знижок; дата створення та дата імпорту розділені.
Ціни з прикладу не копіюються. Контакт і умови взяті зі зразка.

Кнопка «Сформувати PDF-КП» завантажує модуль та ресурси лише на запит;
після генерації з'являється посилання завантаження. Немає даних клієнтів,
серверного збереження, резервування чи надсилання. Зміна підбору скидає
попереднє посилання; PDF, що генерується, відкидається, якщо підбір
змінився під час обробки. Файл фіксує використаний знімок.

Валідація: 1–20 унікальних позицій; цілий слеб, кількість 1–999,
наявність у знімку, підтверджена ціна й обробка, фото. Це не перевірка
кількісного залишку. Непідтверджені позиції, фрагменти й зразки
блокують всю КП, а не мовчки зникають із суми.

`offer.mjs` — незалежний модуль jsPDF з переданими шрифтами й фото;
`offer-download.ts` — браузерне завантаження ресурсів, нормалізація
розміру фото й Blob; джерела логотипа/шрифтів — `src/assets/pdf/README.md`.
Перевірка макета: `node scripts/catalog/preview-offer.mjs` створює
тимчасові PDF у `/tmp` через той самий модуль; відрендерити Poppler.
Перевірено одиночну/багатопозиційну КП, українські символи та великі суми.
Сценарій збереження й поширення в браузері — розділ «PDF-файли та
половини SM Quartz» нижче.

## Генератори двох прайсів — 07.09.2026

Кнопки над фільтрами: натуральний камінь та SM Quartz. Прайс включає
всі позиції відповідної family із inStock=true, крім sample-slab. Пошук,
фільтри та підбір не впливають на склад PDF. Числові залишки не публікуються.
Фрагменти мають власну одиницю ціни; позиції з непідтвердженою ціною
залишаються з написом «Ціну уточнюємо». Ціни за м² і слеб беруться незалежно зі
snapshot, з ПДВ та без знижок; дані старих PDF не замінюють ціни.

Макети за наданими PDF: A4, темна обкладинка, умови, світлі сторінки
по шість карток у дві колонки. Для натурального каменю окремі секції
за підтвердженими колекціями; невідомі не приписуються Bagnara. Для
SM Quartz - код і цінова група/архівна позначка за сторінками 3–7
наданого ALTACO_SM_Quartz_Kyiv_Stock_2026.pdf (SM_GROUPS); новий код
без зіставлення показується без вигаданої групи. Це класифікація зі
зразка, не джерело поточних цін. Логотип SM витягнуто з того ж PDF.

Дві мови; дата формування та дата імпорту вказані окремо. Браузер
завантажує модуль лише після натискання; показує прогрес і посилання
для завантаження готового Blob. Помилка ресурсу зупиняє генерацію,
повторна спроба дозволена. Немає змін складу, клієнтських даних чи
надсилання. Спільний pdf-photo.ts зберігає попередній формат фото КП.

Модулі: pricelist.mjs, pricelist-download.ts, PriceLists.astro.
Перевірки: загальні тести каталогу плюс відбір наявності, збереження
варіантів і непідтверджених полів, незмінність вихідних цін.
preview-pricelists.mjs генерує обидва PDF у /tmp тим самим рендерером.
Перевірено макети PDF; сценарій завантаження/поширення — там само,
розділ «PDF-файли та половини SM Quartz» нижче.


### Оновлення фотографій — 2026-09-07

25 JPEG надано ALTACO без зміни кольору чи фактури. 23 матеріали
(28 варіантів) використовують нові файли в каталозі, картках і PDF.
Carnia, Liskamm (файл Lyskamm) Metropolis Galaxy та обидва варіанти Vega отримали відсутні фото.
Sabbia Beige зіставлено зі складською назвою Sabia Beige. Вихідні назви
файлів і дата отримання збережені в `manifest.json` → `photoSource`;
`reference` лишається джерелом опису типу з попереднього прайсу.
City Brown і City Grey збережено як `sm-city-brown.jpg` та `sm-city-grey.jpg`;
позицій у знімку немає, тому товари та ціни не створено.
Libeccio має 270×169 px, Grigio Nube — 300×188 px, решта — 800×500 px.
Для якісного великого друку першим двом потрібні більші оригінали.

Наступне оновлення: Fusion Taupe — `FusionTaupe_02.jpg` (2048×1568),
Libeccio — `Libecio_02.jpg` (2048×1365), Istria — `Istria.jpg` (800×500).
Низьку роздільність Libeccio усунуто; Istria отримала відсутнє фото.


Назви SM Quartz з обробкою Silk отримують суфікс `Silk` у публічному
знімку та під час імпорту. Каталог, картки, КП і прайси беруть спільне
поле `name`. Ключі зіставлення складської таблиці й ID не змінюються.

## Кошик — 12.09.2026

Замість вбудованого підбору — кнопка «Додати в кошик» і модальний
кошик із лічильником товарних позицій, фотографіями, кількостями,
видаленням, очищенням, сумою та формуванням PDF-КП. Кнопка кошика
залишається доступною при прокручуванні каталогу. Повторне натискання
«У кошику» відкриває кошик, а не видаляє товар.

Кошик доступний у каталозі й на картках матеріалів, обома мовами.
localStorage (`altaco-cart-v1`) містить тільки ID і кількості; ціни,
наявність і характеристики беруться з поточної збірки каталогу.
Збереження діє в межах браузера та origin, не між пристроями або
різними доменами прев’ю. Невідомі ID, зразки, дублікати й невалідні
кількості відкидаються при читанні. Якщо сховище недоступне,
кошик працює в пам’яті сторінки з явним попередженням.
Зміни синхронізуються між вкладками; повернення з browser back cache
підхоплює збережені товари. Без JS кнопки приховані.

PDF зберігає попередні правила: 1–20 цілих слябів у наявності,
підтверджені ціна, обробка й фото. Позиції під замовлення можна
зберегти в кошику, але PDF для них потребує підтвердження менеджером.
Причини блокування показуються біля товарів; часткова КП не створюється.
Зміна кошика скидає PDF і відкидає результат незавершеної генерації.
Роздрібні ціни з ПДВ, без знижок. Кошик не резервує залишки,
не оформлює замовлення, не приймає оплату й не має бекенду.

## PDF-файли та половини SM Quartz — 12.09.2026

Попереднє правило цілих слябів уточнено: товарні позиції SM Quartz
типу slab допускають 0,5–999 слябів із кроком 0,5. Для натурального
каменю залишається ціла кількість 1–999; існуючі фрагменти залишаються
за запитом. Правило спільне для полів кошика, збереження й PDF.
Ціна половини = 50% роздрібної ціни слябу з ПДВ, без знижок.
Сума рядка округлюється один раз до євроцента (0,5 цента вгору).
PDF локалізує дробову кількість, позначає розміри й площу цілого
слябу та зазначає, що розміри половини погоджуються окремо.

Для КП та обох прайсів після генерації доступні «Переглянути»,
«Завантажити PDF» і, за підтримки браузером, «Поділитися PDF».
Спільний модуль створює File із MIME application/pdf і зрозумілим
ім’ям. Web Share передає лише files, без url або тексту з blob-адресою.
canShare перевіряє саме готовий файл; share викликається безпосередньо
окремим натисканням після генерації. Скасування користувачем не є
помилкою. Якщо поширення недоступне або не вдалося, залишається
завантаження з підказкою. Успішне відкриття меню не називаємо
підтвердженням доставки одержувачу.
Зміна кошика скидає всі дії зі старим PDF. Перегенерація одного
прайсу не змінює готовий файл іншого. Серверне збереження не додається.

Власник перевірив збереження й пересилання PDF (КП і прайси) на
реальному iPhone — працює. Android не перевірено. Це підтвердження
користувача на фізичному пристрої, не автоматизований тест.

Телефон ALTACO в КП і в обох прайсах уніфіковано на погоджений
власником номер `+38 097 242 21 21` (`src/catalog/contact.mjs` —
єдине джерело для обох генераторів). Прайс раніше показував інший
номер; замінено. Email і сайт ALTACO не змінювались.

## Шість полів та продаж колишніх зразкових слябів — 12.09.2026

Погоджені поля: пошук; «Виробник / колекція» (НАТУРАЛЬНІ МАТЕРІАЛИ /
SANTAMARGHERITA); тип каменю; обробка; товщина; наявність.
Поле виробника використовує family, а не фактичну collection:
колекції зберігаються в характеристиках і PDF, але окремого фільтра
за ними немає. Сортування фіксоване: наявні першими, далі за назвою.
Застарілі URL-параметри collection, sort, samples прибираються;
пошук, family та інші актуальні фільтри зберігаються. Колір відкладено.

За прямим погодженням власника всі п’ять позицій вихідного розділу
«ЗРАЗКИ | СЛЕБИ» тепер є звичайними натуральними слябами (kind=slab).
Ціни, формати, фото, наявність та сталі ID не змінено. Ключі manifest
зберігають вихідну групу sample-slab для повторного імпорту, який
тепер також видає звичайні товарні сляби. Ручні зразки не імпортуються.
Ці п’ять позицій доступні без прапорця в каталозі, у кошику, КП
та прайсі натурального каменю на загальних умовах.

## Дата актуальності залишків — 13.09.2026

`dataAsOf` лишається `null` — підтвердженої фізичної звірки складу
поки немає; сама дата редагування чи читання таблиці нею не є.
Спільне правило (`src/catalog/freshness.mjs`) визначає два
взаємовиключні написи: за наявності дати — «Залишки станом на
{дата}» / «Stock as of {date}»; за `null` — «Дата актуальності
залишків не підтверджена» / «Stock freshness date has not been
confirmed». Один із двох станів завжди видимий, ніколи обидва разом.

Застосовано в чотирьох місцях: компактний рядок біля фільтрів у
загальному каталозі (`Catalog.astro`), сторінка матеріалу
(`SnapshotNotice.astro`), КП (`offer.mjs`) та обидва прайси
(`pricelist.mjs`). Це окремо від `importedAt` (дата успішного імпорту
знімка) і дати формування конкретного PDF — жодна з них не
підставляється замість `dataAsOf`, усі три позначені окремо.

Підтвердження дати вимагає фізичної звірки залишків на складі
відповідальною людиною на конкретну дату — не редагування файлу
таблиці й не факт її прочитання скриптом імпорту.
