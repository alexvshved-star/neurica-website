# Чек-лист запуску Neurica v1 — PR №10

Підготовлено 16.09.2026 після завершеного незалежного рев'ю
(рішення 085 у docs/05-decisions.md). Цей файл — інструкція для
людини, яка виконує запуск; він не замінює саме рішення злити PR —
це окрема, свідома дія власника.

## Вихідні дані на момент підготовки

- Перевірений SHA робочої гілки (PR head): `5e0b9d20e89201c17a9101825b6f5cbc4b77bb53`.
- SHA `main` на момент підготовки: `fdcd322a2d2d405913362128b6fa31adcc206fc9`.
  Це підтверджений SHA гілки `main` у GitHub. **Припущення, не факт:**
  що саме цей SHA зараз опубікований на production-домені neurica.net —
  ніхто в цій сесії не мав доступу до Netlify-панелі чи API, щоб
  прочитати SHA поточного production deploy напряму. Перед злиттям
  людина з доступом до Netlify має звірити SHA активного deploy
  з `fdcd322`; якщо вони не збігаються — з'ясувати причину до злиття,
  а не після.
- Тег відкату: `neurica-pre-relaunch-2026-09-16`, вказує на `fdcd322`
  (див. розділ «Тег відкату» нижче).
- PR №10: `mergeable_state: clean`, без конфліктів на момент перевірки.

## Перед запуском (виконати до злиття)

1. **Звірити SHA.** `git log -1 origin/main` має показати `fdcd322`.
   Якщо `main` рухнувся вперед після цього чек-листа — зупинитись
   і перевірити, що нового і чи потрібне повторне рев'ю саме тих змін.
2. **Звірити head PR.** `git log -1 origin/claude/neurica-eonyx-relaunch-5e3glx`
   має показати `5e0b9d2`. Якщо ні — код після останнього рев'ю
   змінився; нові коміти не вважати перевіреними, доки їх не прогнали
   через ту саму перевірку (тести, збірка, браузерні перевірки форми
   й манifesту).
3. **Перевірки, які мають бути зеленими на head PR:**
   - GitHub Actions `build` — success;
   - Netlify `deploy-preview` — success (деплой прев'ю зібрався);
   - Netlify `Redirect rules` і `Header rules` — success.
   Перевірити в GitHub: PR №10 → вкладка Checks, або
   `gh pr checks 10` за наявності CLI з доступом.
4. **Домен і гілка публікації.** У Netlify-панелі проєкту
   `beautiful-stardust-816f7f` перевірити: production branch = `main`,
   primary domain = `neurica.net`, build command/publish dir відповідають
   `netlify.toml` цього репозиторію (`npm run build` → `dist`).
   Це налаштування хостингу — тільки читання на цьому кроці,
   нічого не змінювати.
5. **Розпізнавання нових форм Netlify**, якщо доступна панель Forms:
   переконатись, що Netlify вже бачить форми `neurica-pilot-uk` і
   `neurica-pilot-en` (вони з'являються в списку форм проєкту після
   деплою прев'ю, що містить статичний `data-netlify="true"` HTML —
   деталі в docs/05, рішення 083). Якщо форми ще не з'явились у списку —
   це ознака, що form detection не спрацював на цьому деплої;
   з'ясувати причину до продакшн-деплою, не після.
6. **Явне погодження злиття.** Усе перелічене вище — підготовка.
   Саме злиття PR №10 у `main` — окрема дія, яку виконує власник
   або людина з явним дозволом власника, окремо від цього чек-листа.

## Запуск

1. **Спосіб злиття.** Рекомендовано squash-merge або merge commit
   (не rebase-merge) — гілка має 111+ комітів робочої історії, які
   докладно розписані в docs/05; звичайний merge commit зберігає
   можливість швидко знайти межу релізу в `git log main`, а squash
   дає один чистий комівинок на `main` без 111 дрібних кроків.
   Обидва варіанти прийнятні; force-push і rebase гілки `main`
   заборонені в будь-якому випадку.
2. **Після злиття** визначити SHA, який реально пішов у production:
   `git log -1 main` одразу після злиття в GitHub UI, або SHA,
   який Netlify показує як поточний production deploy у власній панелі
   (Netlify показує SHA деплою в списку Deploys). Ці два значення
   мають збігатися.
3. **Підтвердити успішний production deploy**: у Netlify-панелі
   деплой зі статусом «Published» на потрібному SHA; за відсутності
   доступу до панелі — дочекатись, поки production-URL почне
   показувати нову версію (наприклад, за новим текстом манifesту
   чи новою формою на `/about/`), і зафіксувати час і спосіб перевірки.

## Після запуску (production-перевірка)

Контроль-лист сторінок і функцій на `https://neurica.net`:

- [ ] Головна, `/work/` («Роботи»), `/about/` («Контакти») — uk і en.
- [ ] `/work/what-neurica-is/` — новий текст маніфесту, uk і en,
  без старих формулювань «ніщо не мусить працювати» / «другий
  production-контур» (nothing has to work / second production surface).
- [ ] Каталог ALTACO (`/work/altaco-catalog/`) — фільтри, картка товару,
  кошик, генерація КП і обох прайсів (натуральний камінь, SM Quartz).
- [ ] Мовні переходи: перемикач uk/en на кожній сторінці веде
  на відповідний локалізований маршрут.
- [ ] Старі адреси: `/index-en` → `/en/`, `/en` → `/en/` (301,
  перевірити фактичний редирект на production, не лише в `netlify.toml`).
- [ ] `canonical` і `hreflang` на кількох сторінках вказують саме
  на `https://neurica.net/...`, без localhost чи deploy-preview URL
  (перевірити у вихідному HTML production-сторінки, не лише в коді).
- [ ] Вигляд на мобільному екрані (реальний пристрій або емуляція
  ширини ≈375px): головна, каталог, картка товару, форма контактів —
  без горизонтального переповнення.
- [ ] **Контрольна заявка через форму `/about/` або `/en/about/`.**
  Надіслати одну тестову заявку з позначкою, що це перевірка
  (наприклад, ім'я «Test Launch Check», задача — «перевірка форми
  після запуску»), і підтвердити появу цієї заявки в Netlify Forms
  (розділ Forms у панелі проєкту). Це можна зробити ще на етапі
  deploy-preview, якщо Netlify Forms активний для прев'ю цього сайту —
  чекати на production deploy для цього НЕ обов'язково; якщо прев'ю
  вже показує форму в списку Forms, контрольну заявку можна надіслати
  туди раніше. **Хто підтверджує:** власник або людина з доступом
  до Netlify-панелі проєкту `beautiful-stardust-816f7f`, вручну,
  переглядом розділу Forms → Submissions. Ця перевірка НЕ виконана
  в рамках рев'ю 085 — заявку там не надсилали і не встановлювали
  сповіщення.
- [ ] Окремо, якщо налаштоване: email-сповіщення про нову заявку.
  Перевіряє той, хто налаштовував сповіщення в Netlify (Site settings →
  Forms → Form notifications), надсиланням тієї-таки контрольної заявки
  і підтвердженням листа. Якщо сповіщення ще не налаштоване — це
  окрема задача, не блокер запуску сайту.

## Відкат

**Ознаки, за яких потрібен відкат:**
- Production-домен показує зламану сторінку (500-ті помилки,
  порожній build) після деплою нового `main`.
- Каталог ALTACO не завантажується, кошик або генерація PDF
  видають помилку в консолі браузера на production.
- Форма контактів не проходить form detection на production
  (не з'являється в списку Forms) і виправити це швидко не вдається.
- Будь-яка регресія, що блокує основний сценарій (перегляд каталогу,
  контакти, мовні переходи) і не виправляється точковим фіксом
  протягом розумного часу.

**Кроки відновлення через Netlify (якщо цей механізм доступний):**
1. У Netlify-панелі проєкту відкрити список Deploys.
2. Знайти попередній production deploy — той, що відповідає
   `fdcd322` (SHA тегу `neurica-pre-relaunch-2026-09-16`).
3. Використати опцію «Publish deploy» (або аналогічну) на цьому
   попередньому deploy, щоб повернути його як активний production
   без зміни `main` у git.
4. Після цього обов'язково узгодити Git і Netlify: `main` у GitHub
   лишається на новому SHA (Netlify-відкат не рухає git), тож
   потрібно або (а) окремо повідомити команду, що production і `main`
   тимчасово розійшлися, до виправлення й повторного деплою,
   або (б) зробити запасний шлях нижче, якщо потрібно повернути
   і сам `main`.

**Запасний шлях без Netlify UI (revert-коміт/PR):**
1. Не використовувати `git reset` чи force-push на `main`.
2. Створити новий PR з `git revert` на коміт(и) злиття релізу
   (або на весь діапазон, якщо злито squash-комітом — один revert
   того самого squash-комівинка).
3. Пройти той самий шлях перевірки (тести, збірка, чек-и), що й
   для звичайного PR, і змержити revert-PR звичним способом.
4. Це створює новий production deploy зі старим кодом, зберігаючи
   повну історію в git — на відміну від Netlify-відкату, тут `main`
   і production знову збігаються автоматично після деплою revert-PR.

**Тег `neurica-pre-relaunch-2026-09-16` сам по собі:**
- Дозволяє будь-кому знайти точний код старої версії (`git checkout
  neurica-pre-relaunch-2026-09-16`) для порівняння чи ручного відновлення
  файлів коду.
- **Не** відновлює DNS, налаштування Netlify (redirects/headers поза
  git, env-змінні, form notifications), дані вже надісланих форм
  чи будь-який стан, що живе поза цим git-репозиторієм. Відкат
  цих речей — окремі дії в Netlify-панелі й DNS-провайдері,
  цей чек-лист їх не виконує і не гарантує.

## Не входить у цей чек-лист

Оновлення залежностей, чищення застарілих файлів репозиторію,
перенесення в ALTACO Platform, розробка AI-агентів — окремі майбутні
кроки, не частина запуску v1.
